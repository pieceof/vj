#include<bits/stdc++.h>

using namespace std;
const int inf = 0x3f3f3f3f;
const int maxn = 1e6 + 7;
void
int main() {

    int n,m;
    scanf("%d%d", &n, &m);
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < m; i++) {
        scanf("%d", &b[i]);
        flag[i] = ture;
    }


int ans = 0;
dodp(0,0,flag);

return 0;
}

int ans = 0;
dodp(0,0,flag);

return 0;
}